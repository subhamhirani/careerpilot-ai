// Compatibility layer: this codebase uses Lucide icon names while many files
// import from @phosphor-icons/react. Re-export lucide-react so existing imports
// compile and render consistently.
export * from 'lucide-react';
